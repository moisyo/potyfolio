import { SectionTitle } from '../SectionTitle';

export const Works = () => {
  return (
    <section id='works' className='container h-screen bg-black'>
      <SectionTitle title='私について' lead='ABOUT' position='center' />
    </section>
  );
};
